# Locally-injected CA certs for proxied builds — never commit these.
devbox/extra-ca/*.crt
devbox/extra-ca/*.pem

# Node dependencies and lock files
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml
installation_id
